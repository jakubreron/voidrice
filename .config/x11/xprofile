#!/bin/bash

# This file runs when a DM logs you into a graphical session.
# If you use startx/xinit like a Chad, this file will also be sourced.

# Wallpaper with some wal arguments
setbg "" "false" &			                               

# Xresources colors/settings on startup
xrdb ${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources & xrdbpid=$!	 

# Switching caps/esc, adjusting speed, etc...
remaps &		                                                         

autostart=("picom --experimental-backend" dunst unclutter keepassxc redshift fcitx /usr/lib/kdeconnectd "git-sync-repos global" "$ADDITIONAL_AUTOSTART_PROGRAMS")
for program in "${autostart[@]}"; do
  program_without_args=${program%% *}
  pidof -s "$program_without_args" || $program &
done >/dev/null 2>&1

# Weird thingy needed for Kinto to work
# xhost +SI:localuser:root &

# Screen layouts
# layout-primary &
# layout-secondary &
# layout-temporary &

# Ensure that xrdb has finished running before moving on to start the WM/DE.
[ -n "$xrdbpid" ] && wait "$xrdbpid"
