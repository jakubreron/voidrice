#!/usr/bin/env zsh

OS="$(uname -s)"

source $HOME/.config/shell/aliasrc.all

case "$OS" in
  Linux)
    source $HOME/.config/shell/aliasrc.linux
    ;;
  Darwin)
    source $HOME/.config/shell/aliasrc.osx
    ;;
esac
