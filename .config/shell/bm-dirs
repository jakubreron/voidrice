# general
Ap      ${XDG_APPLICATIONS_DIR:-$HOME/Applications}
Dw      ${XDG_DOWNLOAD_DIR:-$HOME/Downloads}
Pi      ${XDG_PICTURES_DIR:-$HOME/Pictures}
Vi      ${XDG_VIDEOS_DIR:-$HOME/Videos}

# documents
Do      ${XDG_DOCUMENTS_DIR:-$HOME/Documents}

# Torrents
Tr      ${XDG_DOCUMENTS_DIR:-$HOME/Documents}/Torrents

# music
Mu      ${XDG_MUSIC_DIR:-$HOME/Music}
Mug     ${XDG_MUSIC_DIR:-$HOME/Music}/general
Mul     ${XDG_MUSIC_DIR:-$HOME/Music}/live
Muo     ${XDG_MUSIC_DIR:-$HOME/Music}/osts
Mua     ${XDG_MUSIC_DIR:-$HOME/Music}/albums
Mut     ${XDG_MUSIC_DIR:-$HOME/Music}/touhou
Muar    ${XDG_MUSIC_DIR:-$HOME/Music}/akatsuki-records

# projects
Pr       $PROJECTS_DIR
Prp      $PROJECTS_DIR/personal
Prs      $PROJECTS_DIR/studies
Prw      $PROJECTS_DIR/work

# Drive
Dv       $HOME/Drive

# dropbox
Dr       ${XDG_DROPBOX_DIR:-$HOME/Dropbox}
Drb      ${XDG_DROPBOX_DIR:-$HOME/Dropbox}/Backups
Drd      ${XDG_DROPBOX_DIR:-$HOME/Dropbox}/Documents
Drdb     ${XDG_DROPBOX_DIR:-$HOME/Dropbox}/Documents/Books
Drp      ${XDG_DROPBOX_DIR:-$HOME/Dropbox}/Pictures
Drpw     ${XDG_DROPBOX_DIR:-$HOME/Dropbox}/Pictures/Wallpapers

# config
Cf       ${XDG_CONFIG_HOME:-$HOME/.config}
Cft      ${XDG_CONFIG_HOME:-$HOME/.config}/tmux
Cfz      ${XDG_CONFIG_HOME:-$HOME/.config}/zsh
Cflv     $LVIM_CONFIG_DIR
# config personal
Cfp      $DOTFILES_DIR
Cfpp     $PKGLIST_DIR
Cfpu     $DOTFILES_DIR/universal
Cfpv     $DOTFILES_DIR/voidrice
Cfpm     $DOTFILES_DIR/macos

# local
L       $HOME/.local
# local bin
Lbn     ${XDG_BIN_HOME:-$HOME/.local/bin}
Lbns    ${XDG_BIN_HOME:-$HOME/.local/bin}/statusbar
Lbng    ${XDG_BIN_HOME:-$HOME/.local/bin}/git
# local share
Lsh     ${XDG_DATA_HOME:-$HOME/.local/share}
Lshlv   $LVIM_SHARE_DIR
# local src
Lsr     ${XDG_SRC_HOME:-$HOME/.local/src}
Lsrdw   ${XDG_SRC_HOME:-$HOME/.local/src}/dwm
Lsrdb   ${XDG_SRC_HOME:-$HOME/.local/src}/dwmblocks
Lsrdm   ${XDG_SRC_HOME:-$HOME/.local/src}/dmenu
Lsrst   ${XDG_SRC_HOME:-$HOME/.local/src}/st
Lsrsx   ${XDG_SRC_HOME:-$HOME/.local/src}/sxiv-flexipatch/

# php
Php     /etc/php/
Phpc    /etc/php/conf.d

# misc
Ca      ${XDG_CACHE_HOME:-$HOME/.cache}
