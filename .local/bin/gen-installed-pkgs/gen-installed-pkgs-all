#!/bin/sh

# TODO: gen all
# TODO: move these osx aliases to separate files shared with other gen-installed-pkgs
# Generate and push packages to repos
# alias \
#   gen-pkg-node="cd $PKGLIST_DIR; gen-pkg-yarn > $PACKAGE_TYPE/yarn.txt" \
#   gen-pkg-brew="cd $PKGLIST_DIR; brew leaves > $PACKAGE_TYPE/brew.txt" \
#   gen-pkg-brew-casks="cd $PKGLIST_DIR; brew list --cask > $PACKAGE_TYPE/brew-casks.txt" \
#   gen-pkg-all="gen-pkg-node; gen-pkg-brew; gen-pkg-brew-casks; git add . && git commit -m'docs($PACKAGE_TYPE/{yarn,brew}): auto-commit changes' && git push origin HEAD"
