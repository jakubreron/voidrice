#!/usr/bin/env bash
if [[  "$STARTUP_DESKTOP" = "Wayland" ]]; then
  hyprctl reload && sleep 1 && hyprctl --batch "keyword monitor LVDS-1,1920x1080,0x0,1.25; keyword monitor DP-2,disabled"
  killall waybar && nohup waybar &
fi
notify-send "💻 Laptop Display"
