#!/usr/bin/env bash
if [[  "$STARTUP_DESKTOP" = "Wayland" ]]; then
  hyprctl --batch "keyword monitor HDMI-A-1,3840x2160,0x0,1.5; keyword monitor eDP-1,disabled; DP-3,disabled"
  killall waybar && nohup waybar &
fi
notify-send "🖥️ External Display"
