#!/usr/bin/env bash
if [[  "$STARTUP_DESKTOP" = "Wayland" ]]; then
  hyprctl --batch "keyword monitor HDMI-A-1,3840x2160,0x0,2; eDP-1,2880x1800,1920x0,2; DP-3,disabled"
  killall waybar && nohup waybar &
  hyprctl reload
fi
notify-send "🖥️ External Display"
