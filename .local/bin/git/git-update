#!/bin/sh

head="${1:-head}"
scope="${2:-scope}"
body="${3:-body}"

msg="$head($scope): $body"

if command -v git &> /dev/null; then
  git add .
  git commit -m "$msg"
  git push origin HEAD

  notify-send "Commited and pushed: \"$msg\""
fi
