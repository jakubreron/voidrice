#!/bin/sh

if command -v lvim &> /dev/null; then
  echo -e "\033[1m⏳ Updating LunarVim...\033[0m"
  [[ -d "$LVIM_SHARE_DIR" ]] && git -C "$LVIM_SHARE_DIR" pull
  [[ -d "$LVIM_CONFIG_DIR" ]] && git -C "$LVIM_CONFIG_DIR" pull

  lvim +LvimUpdate +q
fi
